#!/bin/sh

indent() {
  sed -u 's/^/       /'
}

echo "-----> Creating configuration script for VERSION environment variable"

output_dir=$1/.profile.d
mkdir -p $output_dir
cat << EOF > $output_dir/version.sh
# default VERSION to build source version
export VERSION=\${VERSION:$SOURCE_VERSION}
EOF

